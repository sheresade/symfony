{% extends 'base.html.twig' %}

{% block title %}CarteBancaire{% endblock %}

{% block body %}
    <h1>CarteBancaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carte_bancaire.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ carte_bancaire.numero }}</td>
            </tr>
            <tr>
                <th>DateExpriration</th>
                <td>{{ carte_bancaire.dateExpriration ? carte_bancaire.dateExpriration|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Titule</th>
                <td>{{ carte_bancaire.titule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('carte_bancaire_index') }}">back to list</a>

    <a href="{{ path('carte_bancaire_edit', {'id': carte_bancaire.id}) }}">edit</a>

    {{ include('carte_bancaire/_delete_form.html.twig') }}
{% endblock %}
